name: Build Password Manager APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
      
      - name: Setup Android SDK
        uses: android-actions/setup-android@v2
      
      - name: Build APK
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug

      - name: Create empty marker
        run: |
          ls -la android/app/build/outputs/apk/debug/ || echo "No output dir"
          echo "Build complete" > artifact_marker.txt
      
      - name: Upload marker
        uses: actions/upload-artifact@v4
        with:
          name: password-manager-apk
          path: artifact_marker.txt
