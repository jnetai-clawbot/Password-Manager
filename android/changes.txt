Password Manager v1.2.0 Changes
================================

1. FIX: Import encryption cross-device compatibility
   - Added ensureKeyStoreKey() to create AndroidKeyStore key before first import
   - Import now has per-entry error handling (won't abort entire import on one bad entry)
   - Better feedback: shows imported/skipped/errors counts

2. FEAT: About section now shows actual version from PackageInfo
   - Displays "Version 1.2.0 (3)" instead of hardcoded text

3. FEAT: Check for Updates button in About
   - Queries GitHub API for latest release tag
   - Shows current vs latest version
   - "Download Update" link opens GitHub releases page

4. FEAT: Release APK renamed to Password-Manager.apk

5. CHORE: Version bumped to 1.2.0 (versionCode 3)
